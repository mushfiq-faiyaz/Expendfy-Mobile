export type Expense = {
  id: string
  date: string
  description: string
  amount: number
  createdAt: string
}

export type IncomeEntry = {
  id: string
  amount: number
  description: string
  createdAt: string
}
